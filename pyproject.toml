[project]
name = "amazon-sku-skill"
version = "0.1.0"
description = "Claude Code skill for auditing Ally Audio Amazon listings against competitor SKUs (CommerceIQ take-home)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
]
